# Studio Constance

This is a simple static site scaffold for the `studioconstance` repository. Drop your images into `docs/assets/` and edit `index.html`.

To publish on GitHub Pages, push to the `main` branch and enable Pages with source `main /docs`.
